export default class Character {
    health;
    level;
    hitDamage;
    maxHealth;
    isDead;

    constructor(level) {
    }

    attack(character) {
    }

    takeAHit(damage) {
    }

    die() {
    }
}